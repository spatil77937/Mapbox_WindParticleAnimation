import React from 'react'

const ReportContent = ({dateRange}) => {
  return (
    <div style={{padding:16}}>
    Print {dateRange.start} to {dateRange.end}
    <br/>
    {dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}
    <br/>{dateRange.start} to {dateRange.end}

    <br/>
    </div>
  )
}

export default ReportContent